# 2. 알고리즘 (의사 코드) (10점)

## 1. `main` 함수의 전체 흐름
(프로젝트의 전체 작동 로직을 의사 코드로 작성하세요.)
시작

반복:
    메뉴 출력:
        1. 출석 체크
        2. 출석 명단 보기(관리자)
        0. 종료
    선택 입력 → choice

    만약 choice = 1이면
        출석체크() 호출

    만약 choice = 2이면
        출석명단보기() 호출

    만약 choice = 0이면
        종료 메시지 출력
        반복 종료
끝반복

종료


## 2. 핵심 기능 1: (예: 학생 등록 함수)
```
(출석 체크 기능)
출석체크():
    이름 입력 → name
    출석명단에 name 추가
    "출석 완료" 출력


시작
    만약 현재 학생 수가 최대 인원을 초과하면:
        "등록 불가" 메시지 출력
        함수 종료
    아니면:
        학생 이름 입력받기
        성적 입력받기 (국어, 영어, 수학)
        평균 계산
        배열에 저장
        현재 학생 수 증가
종료
```

## 3. 핵심 기능 2: (예: 학생 검색 함수)
```
(핵심 기능 2의 논리적 절차를 작성하세요.)
```출석 명단 보기 (관리자 비밀번호 필요)
출석명단보기():
    비밀번호 입력 → pw

    만약 pw가 관리자 비밀번호와 다르면:
        "접근 불가" 출력
        함수 종료

    만약 출석명단이 비어있으면:
        "출석한 학생 없음" 출력
        함수 종료

    번호와 함께 출석명단 전체 출력
관리자비밀번호 = "1234"  // 기본 비밀번호

관리자 비밀번호 기능
비밀번호확인():
    pw 입력
    pw == 관리자비밀번호 이면 True 반환
    아니면 False 반환

